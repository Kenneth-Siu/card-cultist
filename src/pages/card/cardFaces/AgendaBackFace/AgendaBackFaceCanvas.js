import AgendaActBackFaceCanvas from "../_AgendaActBackFace/_AgendaActBackFaceCanvas";

export default function AgendaBackFaceCanvas({ face, cardSet }) {
    return AgendaActBackFaceCanvas({ face, cardSet, word: "AGENDA" });
}
