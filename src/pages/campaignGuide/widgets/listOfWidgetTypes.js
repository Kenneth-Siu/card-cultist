import IllustrationWidget from "./illustration/IllustrationWidget";
import SectionHeaderWidget from "./sectionHeader/SectionHeaderWidget";
import TextWidget from "./text/TextWidget";

const listOfWidgetTypes = [TextWidget, SectionHeaderWidget, IllustrationWidget];

export default listOfWidgetTypes;
